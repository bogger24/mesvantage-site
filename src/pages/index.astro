---
import BaseLayout from '../layouts/BaseLayout.astro';
import LeadCaptureModal from '../components/LeadCaptureModal';
---

<BaseLayout title="The Manufacturing Intelligence Platform for Medical Device CDMOs">

  <!-- Hero -->
  <section class="bg-navy text-white pt-24 pb-20 px-6 overflow-hidden relative">
    <div class="absolute inset-0 opacity-[0.04] pointer-events-none"
         style="background-image: linear-gradient(white 1px, transparent 1px), linear-gradient(90deg, white 1px, transparent 1px); background-size: 48px 48px;"></div>
    <div class="max-w-5xl mx-auto text-center relative">
      <div class="inline-flex items-center gap-2 bg-accent/15 border border-accent/30 rounded-full px-4 py-1.5 text-xs font-semibold text-accent tracking-wide uppercase mb-6">
        <span class="w-1.5 h-1.5 rounded-full bg-accent animate-pulse"></span>
        Validated · ISO 13485 · 21 CFR Part 11
      </div>
      <h1 class="text-4xl md:text-6xl font-extrabold leading-[1.05] tracking-tight">
        Every FAI, NCR, audit, and machine —<br class="hidden md:block" /> one validated platform.
      </h1>
      <p class="mt-7 text-lg md:text-xl text-white/70 max-w-3xl mx-auto leading-relaxed">
        Medical device CDMOs spend three weeks preparing for an audit. MESvantage cuts that to four days.
        Built and FDA-validated inside a live 250,000-knee-per-year orthopaedic CDMO.
      </p>
      <div class="mt-10 flex flex-col sm:flex-row gap-4 justify-center">
        <LeadCaptureModal label="Download Product Overview" variant="primary" client:load />
        <a href="/about#contact" class="border border-white/30 hover:border-white text-white font-semibold px-8 py-3.5 rounded-md transition-colors">
          Book a Demo
        </a>
      </div>
    </div>
  </section>

  <!-- Pain recognition -->
  <section class="py-20 px-6 bg-white border-b border-border">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12">
        <span class="text-xs font-bold tracking-widest text-accent uppercase">Does this sound familiar?</span>
        <h2 class="mt-2 text-3xl md:text-4xl font-bold text-navy">The problems every CDMO lives with.</h2>
      </div>
      <div class="grid md:grid-cols-3 gap-6">
        {[
          {
            icon: `<svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" /></svg>`,
            title: 'Audit prep is a 3-week fire drill',
            body: 'Every FDA or notified body visit means weeks of pulling records, chasing paper travellers, and reconstructing device history from spreadsheets and shared drives.',
          },
          {
            icon: `<svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" /></svg>`,
            title: "You can't see your floor in real time",
            body: 'Machine status, OEE, active jobs, operator positions — none of it is visible until someone walks the floor or checks a whiteboard at shift handover.',
          },
          {
            icon: `<svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z" /></svg>`,
            title: 'Your quality system is a shared drive',
            body: 'FAIs, NCRs, CAPAs, incoming inspection results — scattered across SharePoint folders, email threads, and institutional knowledge that leaves when people leave.',
          },
        ].map(card => (
          <div class="bg-surface border border-border rounded-xl p-8">
            <div class="w-10 h-10 rounded-lg bg-accent/10 text-accent flex items-center justify-center mb-5" set:html={card.icon} />
            <h3 class="text-lg font-bold text-navy mb-3">{card.title}</h3>
            <p class="text-ink/70 leading-relaxed text-sm">{card.body}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Quantified outcomes band -->
  <section class="bg-navy/5 border-b border-border">
    <div class="max-w-6xl mx-auto px-6 py-14 grid grid-cols-2 md:grid-cols-4 gap-8 text-center">
      {[
        { stat: '90d', label: 'Time to Live', context: 'From contract to production floor' },
        { stat: '~70%', label: 'Faster Audit Prep', context: 'Weeks of prep becomes days' },
        { stat: '~75%', label: 'FAI Cycle Reduction', context: 'From drawing to approved record' },
        { stat: '€0', label: 'Validation Budget', context: 'IQ/OQ/PQ already passed' },
      ].map(item => (
        <div>
          <div class="text-3xl md:text-4xl font-extrabold text-navy">{item.stat}</div>
          <div class="text-xs uppercase tracking-wider text-ink/60 mt-1 font-semibold">{item.label}</div>
          <div class="text-xs text-ink/40 mt-1">{item.context}</div>
        </div>
      ))}
    </div>
  </section>

  <!-- Architecture / How it works -->
  <section id="how-it-works" class="py-24 px-6 bg-surface">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-14">
        <span class="text-xs font-bold tracking-widest text-accent uppercase">How it works</span>
        <h2 class="mt-2 text-3xl md:text-4xl font-bold text-navy">One platform, every system on your floor.</h2>
        <p class="mt-4 text-ink/60 max-w-2xl mx-auto">
          MESvantage sits at the centre of your manufacturing stack — ingesting data from ERP, quality, machines and metrology, and turning it into validated records, real-time intelligence, and customer-ready outputs.
        </p>
      </div>

      <!-- Architecture diagram (SVG, animated) -->
      <div class="bg-white border border-border rounded-2xl p-6 md:p-10 shadow-sm">
        <svg viewBox="0 0 1100 520" class="w-full h-auto" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="MESvantage architecture diagram">
          <defs>
            <linearGradient id="centerGrad" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stop-color="#0F2D52" />
              <stop offset="100%" stop-color="#1A7FBF" />
            </linearGradient>
            <linearGradient id="flowIn" x1="0%" y1="0%" x2="100%" y2="0%">
              <stop offset="0%" stop-color="#1A7FBF" stop-opacity="0" />
              <stop offset="50%" stop-color="#1A7FBF" stop-opacity="0.8" />
              <stop offset="100%" stop-color="#1A7FBF" stop-opacity="0" />
            </linearGradient>
            <filter id="glow"><feGaussianBlur stdDeviation="3" result="b"/><feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge></filter>
          </defs>

          <!-- Column labels -->
          <text x="160" y="32" text-anchor="middle" fill="#64748B" font-size="11" font-weight="700" letter-spacing="2">YOUR INPUTS</text>
          <text x="550" y="32" text-anchor="middle" fill="#64748B" font-size="11" font-weight="700" letter-spacing="2">MESVANTAGE PLATFORM</text>
          <text x="940" y="32" text-anchor="middle" fill="#64748B" font-size="11" font-weight="700" letter-spacing="2">YOUR OUTPUTS</text>

          <!-- LEFT: Input nodes -->
          {[
            { y: 70, label: 'ERP', sub: 'M1 · SAP · Plex · Infor · Oracle' },
            { y: 150, label: 'QMS', sub: 'Qualio · MasterControl · Greenlight' },
            { y: 230, label: 'Machines', sub: 'OPC-UA · MTConnect · Modbus' },
            { y: 310, label: 'CMMs', sub: 'Renishaw · Hexagon · Zeiss' },
            { y: 390, label: 'Documents', sub: 'OneDrive · SharePoint · Google Drive' },
          ].map(node => (
            <g>
              <rect x="20" y={node.y} width="280" height="60" rx="10" fill="white" stroke="#E2E8F0" stroke-width="1.5"/>
              <text x="40" y={node.y + 28} fill="#0F2D52" font-size="15" font-weight="700">{node.label}</text>
              <text x="40" y={node.y + 47} fill="#64748B" font-size="11">{node.sub}</text>
              <circle cx="300" cy={node.y + 30} r="4" fill="#1A7FBF"/>
            </g>
          ))}

          <!-- CENTER: MESvantage core -->
          <rect x="380" y="100" width="340" height="320" rx="18" fill="url(#centerGrad)" filter="url(#glow)"/>
          <text x="550" y="138" text-anchor="middle" fill="white" font-size="20" font-weight="800" letter-spacing="0.5">MESvantage</text>
          <text x="550" y="158" text-anchor="middle" fill="rgba(255,255,255,0.65)" font-size="11" letter-spacing="1.5">VALIDATED PLATFORM CORE</text>

          {[
            { y: 192, label: 'Device History Records (DHR)' },
            { y: 222, label: 'Genealogy & Traceability' },
            { y: 252, label: 'OEE & Downtime Intelligence' },
            { y: 282, label: 'FAI · Inspections · NCR · CAPA' },
            { y: 312, label: 'Scheduling & Standard Times' },
            { y: 342, label: 'Audit Trail & E-Signatures' },
            { y: 372, label: 'AI Executive Assistant' },
          ].map(item => (
            <g>
              <rect x="408" y={item.y - 12} width="284" height="22" rx="6" fill="rgba(255,255,255,0.08)"/>
              <text x="420" y={item.y + 3} fill="white" font-size="12" font-weight="500">{item.label}</text>
            </g>
          ))}

          <circle cx="380" cy="260" r="4" fill="#1A7FBF"/>
          <circle cx="720" cy="260" r="4" fill="#1A7FBF"/>

          <!-- RIGHT: Output nodes -->
          {[
            { y: 110, label: 'OEM Customer Portals', sub: 'Lot release · DHR · Quality data' },
            { y: 190, label: 'Floor & Mobile', sub: 'PWA · Digital Signage · Operator UX' },
            { y: 270, label: 'Executive Intelligence', sub: 'Power BI · CEO dashboards' },
            { y: 350, label: 'Regulatory Packets', sub: 'IQ/OQ/PQ · Validation evidence' },
          ].map(node => (
            <g>
              <rect x="800" y={node.y} width="280" height="60" rx="10" fill="white" stroke="#E2E8F0" stroke-width="1.5"/>
              <text x="820" y={node.y + 28} fill="#0F2D52" font-size="15" font-weight="700">{node.label}</text>
              <text x="820" y={node.y + 47} fill="#64748B" font-size="11">{node.sub}</text>
              <circle cx="800" cy={node.y + 30} r="4" fill="#1A7FBF"/>
            </g>
          ))}

          <!-- Animated flow paths: inputs -> core -->
          {[100, 180, 260, 340, 420].map((y, i) => (
            <g>
              <path d={`M 304 ${y} Q 342 ${y}, 360 260 T 380 260`} stroke="#CBD5E1" stroke-width="1.5" fill="none" opacity="0.4"/>
              <path d={`M 304 ${y} Q 342 ${y}, 360 260 T 380 260`} stroke="url(#flowIn)" stroke-width="2.5" fill="none" stroke-dasharray="8 200" stroke-dashoffset="0">
                <animate attributeName="stroke-dashoffset" from="0" to="-208" dur={`${2.4 + i * 0.18}s`} repeatCount="indefinite"/>
              </path>
            </g>
          ))}

          <!-- Animated flow paths: core -> outputs -->
          {[140, 220, 300, 380].map((y, i) => (
            <g>
              <path d={`M 720 260 Q 740 260, 760 ${y} T 800 ${y}`} stroke="#CBD5E1" stroke-width="1.5" fill="none" opacity="0.4"/>
              <path d={`M 720 260 Q 740 260, 760 ${y} T 800 ${y}`} stroke="url(#flowIn)" stroke-width="2.5" fill="none" stroke-dasharray="8 200" stroke-dashoffset="0">
                <animate attributeName="stroke-dashoffset" from="0" to="-208" dur={`${2.6 + i * 0.2}s`} repeatCount="indefinite"/>
              </path>
            </g>
          ))}

          <!-- Footer label inside core -->
          <text x="550" y="408" text-anchor="middle" fill="rgba(255,255,255,0.55)" font-size="10" letter-spacing="2">SILOED SAAS · DEDICATED ENVIRONMENT PER CUSTOMER</text>
        </svg>
      </div>

      <p class="text-center text-sm text-ink/50 mt-6 max-w-2xl mx-auto">
        Every customer runs in a fully isolated deployment. No shared compute, no shared databases, no cross-customer data — by design and by audit requirement.
      </p>
    </div>
  </section>

  <!-- Screenshot callout -->
  <section class="py-24 px-6 bg-navy relative overflow-hidden">
    <div class="absolute inset-0 opacity-[0.04] pointer-events-none"
         style="background-image: linear-gradient(white 1px, transparent 1px), linear-gradient(90deg, white 1px, transparent 1px); background-size: 48px 48px;"></div>
    <div class="max-w-6xl mx-auto relative">
      <div class="text-center mb-10">
        <span class="text-xs font-bold tracking-widest text-accent uppercase">Real platform. Live in production.</span>
        <h2 class="mt-2 text-3xl md:text-4xl font-bold text-white">Not a demo. Not a pilot.</h2>
        <p class="mt-4 text-white/60 max-w-xl mx-auto">
          The platform has been running under FDA scrutiny at 250,000 knees per year for two years.
          What you see is what you get.
        </p>
      </div>
      <div class="rounded-2xl overflow-hidden border border-white/10 shadow-2xl">
        <img
          src="/screenshots/intelligence-dashboard.svg"
          alt="MESvantage Intelligence Dashboard — CEO-level visibility across quality, production, and energy"
          class="w-full h-auto block"
          width="1400"
          height="800"
          loading="lazy"
        />
      </div>
      <p class="text-center text-white/30 text-xs mt-4">
        Intelligence module — machine utilisation, quality trends, and predictive alerts. All data anonymised.
      </p>
    </div>
  </section>

  <!-- The gap we fill -->
  <section class="py-20 px-6 bg-white">
    <div class="max-w-6xl mx-auto">
      <h2 class="text-3xl font-bold text-navy text-center mb-12">
        The gap nobody else fills.
      </h2>
      <div class="grid md:grid-cols-3 gap-6">
        {[
          {
            title: 'Purpose-built for CDMOs',
            body: 'Not a generic MES retrofitted to medical. Every workflow — DHR, FAI, CMM, OEE, NCR, lot release — was designed for regulated contract manufacturing from day one.',
          },
          {
            title: 'Validation transferable',
            body: 'IQ/OQ/PQ baseline already passed in a live regulated environment. Validation evidence transfers with the platform. Skip the 12-month validation programme.',
          },
          {
            title: 'Production-grade',
            body: 'Stress-tested at scale, 24/7, under FDA scrutiny. Every feature has run on a real production floor — not a lab, not a pilot, not a demo environment.',
          },
        ].map(card => (
          <div class="bg-surface border border-border rounded-xl p-8">
            <h3 class="text-lg font-bold text-navy mb-3">{card.title}</h3>
            <p class="text-ink/70 leading-relaxed">{card.body}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Founder quote -->
  <section class="py-20 px-6 bg-surface border-b border-border">
    <div class="max-w-3xl mx-auto text-center">
      <svg class="w-10 h-10 text-accent/40 mx-auto mb-6" fill="currentColor" viewBox="0 0 32 32">
        <path d="M10 8C6.686 8 4 10.686 4 14v10h10V14H7c0-1.657 1.343-3 3-3V8zm18 0c-3.314 0-6 2.686-6 6v10h10V14h-7c0-1.657 1.343-3 3-3V8z"/>
      </svg>
      <blockquote class="text-xl md:text-2xl font-medium text-navy leading-relaxed">
        "We built the MES we couldn't find on the market. It's been running live in our plant for two years —
        250,000 knee systems a year, fully under FDA scrutiny. That's not a pilot. That's the product."
      </blockquote>
      <div class="mt-8 flex items-center justify-center gap-4">
        <div class="w-12 h-12 rounded-full bg-navy/10 flex items-center justify-center text-navy font-bold text-lg">PB</div>
        <div class="text-left">
          <div class="font-bold text-navy">Patrick Byrnes</div>
          <div class="text-sm text-ink/60">CEO, Croom Medical — Founding Customer</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Mockup scene: Operations dashboard (HTML/CSS, MESvantage-branded, fake data) -->
  <section class="py-24 px-6 bg-navy">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12">
        <span class="text-xs font-bold tracking-widest text-accent uppercase">The platform</span>
        <h2 class="mt-2 text-3xl md:text-4xl font-bold text-white">Real-time intelligence. Without the Excel.</h2>
        <p class="mt-4 text-white/60 max-w-2xl mx-auto">
          See your shop floor the way your CEO wants to see it: live, drillable, audit-ready.
        </p>
      </div>

      <!-- Browser chrome -->
      <div class="rounded-xl overflow-hidden shadow-2xl border border-white/10">
        <div class="bg-[#1e2a3a] px-4 py-2.5 flex items-center gap-3">
          <div class="flex gap-1.5">
            <span class="w-3 h-3 rounded-full bg-red-400/70"></span>
            <span class="w-3 h-3 rounded-full bg-yellow-400/70"></span>
            <span class="w-3 h-3 rounded-full bg-green-400/70"></span>
          </div>
          <div class="flex-1 bg-white/10 rounded px-3 py-1 text-xs text-white/40 font-mono">
            app.mesvantage.com / operations
          </div>
        </div>

        <!-- Mockup body -->
        <div class="bg-[#F8F9FB] flex" style="min-height: 480px;">
          <!-- Sidebar -->
          <aside class="w-56 bg-[#0F2D52] text-white py-4 hidden md:block">
            <div class="px-5 pb-4 border-b border-white/10">
              <div class="font-extrabold tracking-tight text-lg">MESvantage</div>
              <div class="text-[10px] text-accent uppercase tracking-wider mt-0.5">Operations</div>
            </div>
            <nav class="mt-3 text-sm">
              {[
                { label: 'Operations', active: true },
                { label: 'Quality' },
                { label: 'Genealogy' },
                { label: 'Scheduling' },
                { label: 'OEE Monitor' },
                { label: 'Inspections' },
                { label: 'Documents' },
                { label: 'Assistant' },
              ].map(item => (
                <div class={`px-5 py-2 ${item.active ? 'bg-accent/20 border-l-2 border-accent text-white' : 'text-white/60'}`}>
                  {item.label}
                </div>
              ))}
            </nav>
            <div class="absolute"></div>
          </aside>

          <!-- Main content -->
          <div class="flex-1 p-6">
            <div class="flex justify-between items-start mb-6">
              <div>
                <h3 class="text-xl font-bold text-navy">Operations Dashboard</h3>
                <p class="text-xs text-ink/50 mt-0.5">Live · Sample data · Updated 14:06</p>
              </div>
              <div class="flex items-center gap-2 text-xs">
                <span class="bg-green-100 text-green-700 px-2 py-1 rounded font-semibold">● Synced</span>
                <span class="bg-accent/10 text-accent px-2 py-1 rounded font-semibold">FDA 21 CFR Part 11</span>
              </div>
            </div>

            <!-- KPI cards -->
            <div class="grid grid-cols-2 md:grid-cols-4 gap-3 mb-5">
              {[
                { label: 'Output today', val: '1,248', sub: 'units · 96% target', tone: 'ok' },
                { label: 'OEE', val: '78.4%', sub: 'fleet-wide', tone: 'ok' },
                { label: 'FPY', val: '99.2%', sub: '24h rolling', tone: 'ok' },
                { label: 'Open NCRs', val: '3', sub: '1 awaiting CAPA', tone: 'warn' },
              ].map(kpi => (
                <div class="bg-white border border-border rounded-lg p-3">
                  <div class="text-[10px] uppercase tracking-wider text-ink/50 font-semibold">{kpi.label}</div>
                  <div class={`text-2xl font-extrabold mt-1 ${kpi.tone === 'warn' ? 'text-amber-600' : 'text-navy'}`}>{kpi.val}</div>
                  <div class="text-[10px] text-ink/50 mt-0.5">{kpi.sub}</div>
                </div>
              ))}
            </div>

            <!-- Two-column: chart + cell list -->
            <div class="grid md:grid-cols-3 gap-3">
              <div class="md:col-span-2 bg-white border border-border rounded-lg p-4">
                <div class="flex justify-between items-center mb-3">
                  <div class="text-xs font-bold text-navy">OEE — last 12 hours</div>
                  <div class="text-[10px] text-ink/50">Availability · Performance · Quality</div>
                </div>
                <!-- Inline chart -->
                <svg viewBox="0 0 400 110" class="w-full h-24">
                  <defs>
                    <linearGradient id="chartFill" x1="0" x2="0" y1="0" y2="1">
                      <stop offset="0%" stop-color="#1A7FBF" stop-opacity="0.35"/>
                      <stop offset="100%" stop-color="#1A7FBF" stop-opacity="0"/>
                    </linearGradient>
                  </defs>
                  <path d="M 0 70 L 33 60 L 66 65 L 99 50 L 132 45 L 165 55 L 198 38 L 231 42 L 264 30 L 297 35 L 330 28 L 363 25 L 400 30 L 400 110 L 0 110 Z" fill="url(#chartFill)"/>
                  <path d="M 0 70 L 33 60 L 66 65 L 99 50 L 132 45 L 165 55 L 198 38 L 231 42 L 264 30 L 297 35 L 330 28 L 363 25 L 400 30" fill="none" stroke="#1A7FBF" stroke-width="2"/>
                </svg>
                <div class="flex justify-between text-[9px] text-ink/40 font-mono mt-1">
                  <span>02:00</span><span>06:00</span><span>10:00</span><span>14:00</span>
                </div>
              </div>
              <div class="bg-white border border-border rounded-lg p-4">
                <div class="text-xs font-bold text-navy mb-3">Cell status</div>
                <div class="space-y-2">
                  {[
                    { cell: 'Cell 01', state: 'Running', tone: 'ok' },
                    { cell: 'Cell 02', state: 'Running', tone: 'ok' },
                    { cell: 'Cell 03', state: 'Setup', tone: 'warn' },
                    { cell: 'Cell 04', state: 'Running', tone: 'ok' },
                    { cell: 'CMM Bay', state: 'Idle', tone: 'idle' },
                  ].map(row => (
                    <div class="flex justify-between items-center text-xs">
                      <span class="text-ink/70 font-medium">{row.cell}</span>
                      <span class={
                        row.tone === 'ok' ? 'text-green-600 font-semibold' :
                        row.tone === 'warn' ? 'text-amber-600 font-semibold' :
                        'text-ink/40 font-semibold'
                      }>● {row.state}</span>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <p class="text-center text-xs text-white/40 mt-6 italic">
        Sample data shown. Each MESvantage deployment runs in its own isolated environment.
      </p>
    </div>
  </section>

  <!-- Mockup scene: Machine Intelligence -->
  <section class="py-24 px-6 bg-surface">
    <div class="max-w-6xl mx-auto">
      <div class="grid md:grid-cols-2 gap-12 items-center mb-12">
        <div>
          <span class="text-xs font-bold tracking-widest text-accent uppercase">Machine Intelligence</span>
          <h2 class="mt-2 text-3xl md:text-4xl font-bold text-navy leading-tight">
            Every machine, every shift, every alert — predictive.
          </h2>
          <p class="mt-4 text-ink/70 leading-relaxed">
            Live ingestion from OPC-UA, MTConnect and Modbus. ML models trained per machine class predict thermal drift, dimensional risk, and tool wear before they break a part — not after.
          </p>
          <ul class="mt-6 space-y-2 text-sm">
            {[
              'Real-time OEE per cell, work centre, and fleet',
              'Thermal-drift prediction with 24-hour horizon',
              'Auto-generated maintenance work orders',
              'Closed-loop feedback to scheduling',
              'Edge ML — no cloud round-trip required',
            ].map(item => (
              <li class="flex items-start gap-2 text-ink/80">
                <span class="text-accent font-bold mt-0.5">▸</span>{item}
              </li>
            ))}
          </ul>
        </div>

        <!-- Browser mockup -->
        <div class="rounded-xl overflow-hidden shadow-2xl border border-border">
          <div class="bg-[#1e2a3a] px-4 py-2.5 flex items-center gap-3">
            <div class="flex gap-1.5">
              <span class="w-3 h-3 rounded-full bg-red-400/70"></span>
              <span class="w-3 h-3 rounded-full bg-yellow-400/70"></span>
              <span class="w-3 h-3 rounded-full bg-green-400/70"></span>
            </div>
            <div class="flex-1 bg-white/10 rounded px-3 py-1 text-xs text-white/40 font-mono">
              app.mesvantage.com / machine-intel
            </div>
          </div>
          <div class="bg-[#0F2D52] p-5">
            <!-- AI alert -->
            <div class="bg-amber-500/10 border border-amber-500/30 rounded-lg px-3 py-2 mb-4 flex items-start gap-2">
              <span class="text-amber-400 mt-0.5 text-xs">⚠</span>
              <div class="text-[11px] text-white/85 leading-snug">
                <span class="font-semibold text-amber-300">Predictive alert.</span> Cell 03 thermal drift trending. 73% probability of dimensional drift in next 4h. Maintenance scheduled 16:30.
              </div>
            </div>

            <!-- Machine cards grid -->
            <div class="grid grid-cols-2 gap-2">
              {[
                { name: 'Cell 01', oee: 84, status: 'Running', tone: 'ok', part: 'PT-2049-B' },
                { name: 'Cell 02', oee: 79, status: 'Running', tone: 'ok', part: 'PT-2103-A' },
                { name: 'Cell 03', oee: 62, status: 'Setup', tone: 'warn', part: 'PT-2049-B' },
                { name: 'Cell 04', oee: 88, status: 'Running', tone: 'ok', part: 'PT-2210-C' },
                { name: 'CMM-Bay-1', oee: 71, status: 'Running', tone: 'ok', part: 'QC Queue' },
                { name: 'VMC-09', oee: 45, status: 'Idle', tone: 'idle', part: '—' },
              ].map(m => (
                <div class="bg-white/5 border border-white/10 rounded-md p-2.5">
                  <div class="flex justify-between items-start mb-1.5">
                    <div>
                      <div class="text-white text-[11px] font-bold tracking-wide">{m.name}</div>
                      <div class="text-white/40 text-[9px] font-mono">{m.part}</div>
                    </div>
                    <span class={
                      m.tone === 'ok' ? 'text-[9px] bg-green-500/20 text-green-300 px-1.5 py-0.5 rounded font-semibold' :
                      m.tone === 'warn' ? 'text-[9px] bg-amber-500/20 text-amber-300 px-1.5 py-0.5 rounded font-semibold' :
                      'text-[9px] bg-white/10 text-white/40 px-1.5 py-0.5 rounded font-semibold'
                    }>● {m.status}</span>
                  </div>
                  <div class="flex items-end gap-2">
                    <div class={`text-2xl font-extrabold leading-none ${m.tone === 'warn' ? 'text-amber-300' : m.tone === 'idle' ? 'text-white/40' : 'text-white'}`}>{m.oee}<span class="text-[10px] text-white/40 font-normal">%</span></div>
                    <!-- Sparkline -->
                    <svg viewBox="0 0 60 16" class="flex-1 h-4 opacity-70">
                      <polyline
                        fill="none"
                        stroke={m.tone === 'warn' ? '#FCD34D' : m.tone === 'idle' ? '#64748B' : '#1A7FBF'}
                        stroke-width="1.5"
                        points={
                          m.tone === 'warn' ? '0,4 10,5 20,7 30,8 40,10 50,11 60,12' :
                          m.tone === 'idle' ? '0,8 10,9 20,8 30,9 40,8 50,9 60,9' :
                          '0,10 10,8 20,9 30,6 40,7 50,4 60,3'
                        }
                      />
                    </svg>
                  </div>
                  <div class="text-[9px] text-white/40 mt-1">MTBF 18.4h · MTTR 22m</div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Mockup scene: AI Executive Assistant -->
  <section class="py-24 px-6 bg-white">
    <div class="max-w-6xl mx-auto">
      <div class="grid md:grid-cols-2 gap-12 items-center">
        <!-- Browser mockup (dark) -->
        <div class="rounded-xl overflow-hidden shadow-2xl border border-border order-2 md:order-1">
          <div class="bg-[#0a0f1a] px-4 py-2.5 flex items-center gap-3">
            <div class="flex gap-1.5">
              <span class="w-3 h-3 rounded-full bg-red-400/70"></span>
              <span class="w-3 h-3 rounded-full bg-yellow-400/70"></span>
              <span class="w-3 h-3 rounded-full bg-green-400/70"></span>
            </div>
            <div class="flex-1 bg-white/5 rounded px-3 py-1 text-xs text-white/40 font-mono">
              app.mesvantage.com / assistant
            </div>
          </div>
          <div class="bg-[#0a0f1a] p-5 text-white" style="min-height: 460px;">
            <!-- User question -->
            <div class="flex justify-end mb-4">
              <div class="bg-accent/20 border border-accent/40 rounded-2xl rounded-tr-sm px-4 py-2 max-w-[85%]">
                <div class="text-[12px] text-white/90 leading-snug">Why did Cell 03 miss target this week?</div>
              </div>
            </div>

            <!-- Assistant response -->
            <div class="flex gap-2 mb-3">
              <div class="w-7 h-7 rounded-full bg-gradient-to-br from-accent to-blue-700 flex items-center justify-center text-[10px] font-bold flex-shrink-0">M</div>
              <div class="flex-1 space-y-3">
                <div class="text-[11px] text-white/85 leading-relaxed">
                  Cell 03 hit <span class="font-bold text-amber-300">87.4%</span> of target this week vs. fleet average <span class="font-bold text-green-400">96.1%</span>. Three contributing factors:
                </div>

                <!-- Inline chart -->
                <div class="bg-white/5 border border-white/10 rounded-lg p-3">
                  <div class="text-[10px] text-white/50 mb-2 font-semibold">Cell 03 — Output vs. Target (this week)</div>
                  <svg viewBox="0 0 280 60" class="w-full h-14">
                    <line x1="0" y1="20" x2="280" y2="20" stroke="#1A7FBF" stroke-width="1" stroke-dasharray="3 3" opacity="0.5"/>
                    <text x="0" y="16" fill="#1A7FBF" font-size="7" opacity="0.7">target</text>
                    {[40, 38, 32, 45, 28, 42, 36].map((v, i) => (
                      <rect x={i * 38 + 4} y={50 - v} width="28" height={v} fill={v < 35 ? '#FCD34D' : '#1A7FBF'} opacity="0.85" rx="1"/>
                    ))}
                    {['M','T','W','T','F','S','S'].map((d, i) => (
                      <text x={i * 38 + 18} y="58" fill="#64748B" font-size="7" text-anchor="middle">{d}</text>
                    ))}
                  </svg>
                </div>

                <ol class="text-[11px] text-white/80 leading-relaxed space-y-1.5 list-decimal list-inside">
                  <li><span class="font-semibold text-white">Tool change overrun</span> on Wed — operator Smith K. flagged thermal drift; 47min unscheduled stop.</li>
                  <li><span class="font-semibold text-white">PT-2049-B setup time</span> averaging 28% above Standard Time. Linked NCR <a class="text-accent underline">NCR-1147</a> still open.</li>
                  <li><span class="font-semibold text-white">Operator coverage</span> — 3 shifts ran short by 1 trained operator (Qualio cert gap).</li>
                </ol>

                <div class="bg-accent/10 border border-accent/30 rounded-lg p-3">
                  <div class="text-[10px] text-accent font-bold uppercase tracking-wide mb-1">Recommendation</div>
                  <div class="text-[11px] text-white/85 leading-snug">Approve recalibration WO #4892 (drafted) and schedule operator Patel R. for PT-2049-B graduation Thursday. Estimated recovery: <span class="font-bold text-green-400">+8.2 OEE pts</span> next week.</div>
                </div>

                <!-- Sources pill -->
                <div class="flex flex-wrap gap-1.5 pt-1">
                  {['OEE telemetry', 'NCR-1147', 'Standard Times', 'Qualio training', 'WO #4892'].map(src => (
                    <span class="text-[9px] bg-white/5 border border-white/10 text-white/60 px-2 py-0.5 rounded font-mono">{src}</span>
                  ))}
                </div>
              </div>
            </div>

            <!-- Input -->
            <div class="mt-5 bg-white/5 border border-white/10 rounded-full px-4 py-2 flex items-center gap-3">
              <span class="text-white/30 text-[11px]">Ask anything about your floor…</span>
              <span class="ml-auto text-[10px] text-white/40 font-mono">⌘ K</span>
            </div>
          </div>
        </div>

        <!-- Copy -->
        <div class="order-1 md:order-2">
          <span class="text-xs font-bold tracking-widest text-accent uppercase">Executive Assistant</span>
          <h2 class="mt-2 text-3xl md:text-4xl font-bold text-navy leading-tight">
            Ask the question. Skip the dashboard.
          </h2>
          <p class="mt-4 text-ink/70 leading-relaxed">
            A purpose-built LLM trained on your shop floor. It speaks DHR, FAI, NCR, OEE and ERP — and it cites every source. Auditors love it. Operators ask it everything.
          </p>
          <ul class="mt-6 space-y-2 text-sm">
            {[
              'Trained on your data, never co-mingled with another customer',
              'Cites source records for every claim — audit-defensible',
              'Drafts work orders, NCRs and CAPAs from natural-language requests',
              'Available to every authorised role — operator to CEO',
              'Runs in your isolated environment — your data never leaves',
            ].map(item => (
              <li class="flex items-start gap-2 text-ink/80">
                <span class="text-accent font-bold mt-0.5">▸</span>{item}
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Mockup scene: Genealogy & DHR -->
  <section class="py-24 px-6 bg-navy text-white">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12">
        <span class="text-xs font-bold tracking-widest text-accent uppercase">Genealogy & DHR</span>
        <h2 class="mt-2 text-3xl md:text-4xl font-bold text-white">Every part. Every operation. Every signature.</h2>
        <p class="mt-4 text-white/60 max-w-2xl mx-auto">
          Trace a single serial number from raw stock to OEM dispatch in two clicks. Audit-ready out of the box.
        </p>
      </div>

      <!-- Browser mockup -->
      <div class="rounded-xl overflow-hidden shadow-2xl border border-white/10">
        <div class="bg-[#1e2a3a] px-4 py-2.5 flex items-center gap-3">
          <div class="flex gap-1.5">
            <span class="w-3 h-3 rounded-full bg-red-400/70"></span>
            <span class="w-3 h-3 rounded-full bg-yellow-400/70"></span>
            <span class="w-3 h-3 rounded-full bg-green-400/70"></span>
          </div>
          <div class="flex-1 bg-white/10 rounded px-3 py-1 text-xs text-white/40 font-mono">
            app.mesvantage.com / genealogy / SN-A4729-00184
          </div>
        </div>

        <div class="bg-[#F8F9FB] p-6">
          <!-- Header -->
          <div class="flex flex-wrap items-start justify-between mb-6 gap-3">
            <div>
              <div class="text-[10px] uppercase tracking-wider text-ink/50 font-semibold">Serial number</div>
              <div class="text-2xl font-extrabold text-navy">SN-A4729-00184</div>
              <div class="text-xs text-ink/60 mt-0.5">Part PT-2049-B · Lot LT-2026-Q2-018 · Released 12 May 2026</div>
            </div>
            <div class="flex flex-wrap gap-2">
              <span class="text-[10px] bg-green-100 text-green-700 px-2 py-1 rounded font-semibold">● Released to OEM</span>
              <span class="text-[10px] bg-accent/10 text-accent px-2 py-1 rounded font-semibold">FDA 21 CFR Part 11</span>
              <span class="text-[10px] bg-navy/10 text-navy px-2 py-1 rounded font-semibold">DHR complete</span>
            </div>
          </div>

          <!-- Timeline -->
          <div class="relative">
            <div class="absolute left-[15px] top-2 bottom-2 w-px bg-border"></div>
            {[
              { time: '08 May · 14:32', op: 'Lot received', detail: 'Supplier SUP-217 · CoC verified · Incoming Qty 240', actor: 'Receiving · auto', tone: 'done' },
              { time: '08 May · 16:15', op: 'Incoming inspection', detail: 'Hardness ✓ · Surface ✓ · Dimensional ✓ · Sample n=8', actor: 'Inspector Doyle M.', tone: 'done' },
              { time: '09 May · 09:00', op: 'First Article Inspection', detail: 'CMM Renishaw EQUATOR · 32/32 features in tolerance · FAI-2026-0418', actor: 'Inspector Patel R.', tone: 'done' },
              { time: '10 May · 14:22 → 15:45', op: 'Operation 010 — VMC turning', detail: 'VMC-09 · Program PRG-2049-010-r4 · Tool offsets logged · Cycle 18m24s', actor: 'Operator Smith K.', tone: 'done' },
              { time: '10 May · 16:00 → 17:30', op: 'Operation 020 — Mill & finish', detail: 'Cell 03 · Program PRG-2049-020-r2 · Coolant Δ flagged · NCR-1147 raised', actor: 'Operator Jones K.', tone: 'warn' },
              { time: '11 May · 10:15', op: 'Dimensional CMM', detail: 'Hexagon Global S · 18/18 features ✓ · Report HX-2026-1184', actor: 'Inspector Patel R.', tone: 'done' },
              { time: '11 May · 13:42', op: 'Final inspection', detail: 'Visual ✓ · Marking ✓ · Packaging ✓', actor: 'Inspector Doyle M.', tone: 'done' },
              { time: '12 May · 09:14', op: 'DHR signed & locked', detail: 'Electronic signature · Hash 0x9A4F…E218', actor: 'QA Manager Walsh F.', tone: 'done' },
              { time: '12 May · 14:00', op: 'Released to OEM portal', detail: 'Lot release packet generated · transmitted to customer portal', actor: 'auto · MESvantage', tone: 'done' },
            ].map(step => (
              <div class="relative pl-10 pb-4 last:pb-0">
                <div class={`absolute left-2 top-1 w-3 h-3 rounded-full border-2 border-white ${step.tone === 'warn' ? 'bg-amber-500' : 'bg-accent'}`}></div>
                <div class="text-[10px] font-mono text-ink/50">{step.time}</div>
                <div class="font-semibold text-navy text-sm">{step.op}</div>
                <div class="text-[11px] text-ink/60 mt-0.5">{step.detail}</div>
                <div class="text-[10px] text-ink/40 mt-0.5 italic">{step.actor}</div>
              </div>
            ))}
          </div>

          <!-- Footer pills -->
          <div class="flex flex-wrap gap-2 mt-6 pt-4 border-t border-border">
            <span class="text-[10px] bg-white border border-border text-navy px-2 py-1 rounded font-semibold">Linked NCR · NCR-1147</span>
            <span class="text-[10px] bg-white border border-border text-navy px-2 py-1 rounded font-semibold">9 e-signatures</span>
            <span class="text-[10px] bg-white border border-border text-navy px-2 py-1 rounded font-semibold">2 CMM reports attached</span>
            <span class="text-[10px] bg-white border border-border text-navy px-2 py-1 rounded font-semibold">FAI-2026-0418</span>
            <span class="text-[10px] bg-white border border-border text-navy px-2 py-1 rounded font-semibold">CoC available</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Module highlights -->
  <section class="py-24 px-6 bg-white border-b border-border">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-14">
        <span class="text-xs font-bold tracking-widest text-accent uppercase">The platform</span>
        <h2 class="mt-2 text-3xl md:text-4xl font-bold text-navy">50+ validated modules. Six domains.</h2>
        <p class="mt-4 text-ink/60 max-w-xl mx-auto">
          Every module was designed for regulated contract manufacturing — not retrofitted from a generic platform.
        </p>
      </div>
      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {[
          {
            domain: 'Quality', num: '01',
            icon: `<svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>`,
            outcome: 'From incoming inspection to lot release — every quality record signed, traceable, and audit-ready under 21 CFR Part 11.',
          },
          {
            domain: 'Production', num: '02',
            icon: `<svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" /><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" /></svg>`,
            outcome: 'Real-time OEE, scheduling, and in-process documentation that eliminates paper travellers and floor clipboards.',
          },
          {
            domain: 'Metrology', num: '03',
            icon: `<svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" /></svg>`,
            outcome: 'CMM results parsed automatically from Renishaw, Hexagon, and Zeiss — tolerance failures flagged before the part leaves the machine.',
          },
          {
            domain: 'Training', num: '04',
            icon: `<svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" /></svg>`,
            outcome: 'Operators can only run what they are trained on. Trainer sign-off, competency expiry, and enforcement — fully automated.',
          },
          {
            domain: 'Intelligence', num: '05',
            icon: `<svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg>`,
            outcome: 'CEO-level visibility: machine utilisation, quality trends, energy spend, and predictive alerts — without a BI team or data analyst.',
          },
          {
            domain: 'Platform', num: '06',
            icon: `<svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 3H5a2 2 0 00-2 2v4m6-6h10a2 2 0 012 2v4M9 3v18m0 0h10a2 2 0 002-2V9M9 21H5a2 2 0 01-2-2V9m0 0h18" /></svg>`,
            outcome: 'Mobile PWA, digital signage, audit trail, e-signatures, and role-based access — the regulated infrastructure that makes the rest possible.',
          },
        ].map(m => (
          <div class="bg-surface border border-border rounded-xl p-8 hover:border-accent/40 hover:shadow-sm transition-all">
            <div class="flex items-center gap-3 mb-5">
              <div class="w-10 h-10 rounded-lg bg-accent/10 text-accent flex items-center justify-center flex-shrink-0" set:html={m.icon} />
              <div>
                <div class="text-xs font-bold tracking-wider text-ink/30 uppercase">Domain {m.num}</div>
                <div class="text-base font-bold text-navy leading-tight">{m.domain}</div>
              </div>
            </div>
            <p class="text-ink/70 leading-relaxed text-sm">{m.outcome}</p>
            <a href={`/product#${m.domain.toLowerCase()}`} class="inline-flex items-center gap-1 mt-4 text-xs font-semibold text-accent hover:text-accent-dark transition-colors">
              See modules <svg class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7" /></svg>
            </a>
          </div>
        ))}
      </div>
      <div class="mt-10 text-center">
        <a href="/product" class="text-sm font-semibold text-accent hover:text-accent-dark transition-colors">
          View all 50+ modules →
        </a>
      </div>
    </div>
  </section>

  <!-- Closing CTA -->
  <section class="py-24 px-6 bg-navy relative overflow-hidden">
    <div class="absolute inset-0 opacity-[0.04] pointer-events-none"
         style="background-image: linear-gradient(white 1px, transparent 1px), linear-gradient(90deg, white 1px, transparent 1px); background-size: 48px 48px;"></div>
    <div class="max-w-3xl mx-auto text-center relative">
      <span class="text-xs font-bold tracking-widest text-accent uppercase">Get started</span>
      <h2 class="mt-3 text-3xl md:text-4xl font-bold text-white leading-tight">
        See exactly what MESvantage<br class="hidden md:block" /> would look like in your plant.
      </h2>
      <p class="mt-5 text-white/60 text-lg max-w-xl mx-auto">
        Download the product overview — a 2-page summary of every module, compliance credentials, and implementation timeline.
      </p>
      <div class="mt-10 flex flex-col sm:flex-row gap-4 justify-center">
        <LeadCaptureModal label="Download Product Overview" variant="primary" client:load />
        <a href="/about#contact" class="border border-white/30 hover:border-white text-white font-semibold px-8 py-3.5 rounded-md transition-colors">
          Book a Demo Instead
        </a>
      </div>
    </div>
  </section>

</BaseLayout>
